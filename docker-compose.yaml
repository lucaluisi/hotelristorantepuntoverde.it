services:
  hotelristorantepuntoverde-it:
    image: hotelristorantepuntoverde-it
    container_name: hotelristorantepuntoverde-it
    build: .
    networks:
      - network
    ports:
      - 3000:3000
    restart: always

networks:
  network: