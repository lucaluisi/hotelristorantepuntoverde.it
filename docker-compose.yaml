services:
  hotelristorantepuntoverde-it:
    image: hotelristorantepuntoverde-it:latest
    container_name: hotelristorantepuntoverde-it
    networks:
      - network
    expose:
      - 3000
    restart: always

networks:
  network: